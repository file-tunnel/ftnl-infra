# Supabase infrastructure module

Canonical Supabase provider overlay for `file-tunnel`. Persistence contracts remain in `ftnl-lib-core`; this module owns Supabase-specific provider material only.

Provider-native working directory: `modules/supabase/projects/<project-ref>`.
